<%= form_for(create_goal_path, html: {id: "new-goal", autocomplete: "off", :class => "form"}) do |f| %>
    <div class="form-section">
        <h4 class="form-section-header">Tell us about the race.</h4>
        <div class="form-line">
            <p class="note">It's happening on</p>
            <input value="10/12/2015"/>
            <p class="note">in</p>
            <input placeholder="Chicago"/>
            <select>
                <option>IL</option>
            </select>
        </div>

        <button type="submit" class="button accent">Continue</button>
    </div>
<% end %>

<%= form_for(create_goal_path, html: {id: "new-goal", autocomplete: "off", :class => "form"}) do |f| %>
    <div class="form-section">
        <h4 class="form-section-header">Enter a description for your goal.</h4>
        <p class="note" style="padding-bottom: 0;">This will be displayed to everyone who comes to donate to your fundraiser.</p>
        <p class="note">Tell them about yourself, and what is driving you to cross the finish line!</p>
        <textarea></textarea>

        <button type="submit" class="button accent">Continue</button>
    </div>
<% end %>

<%= form_for(create_goal_path, html: {id: "new-goal", autocomplete: "off", :class => "form"}) do |f| %>
    <div class="form-section">
        <h4 class="form-section-header">Have you done this before?</h4>
        <p class="note">Skip this section if you haven't, but if you have, enter your previous times.</p>
        <textarea></textarea>

        <button type="submit" class="button accent">Continue</button>
        <div class="clearfix"></div>
    </div>
    <div class="form-section">
        <h4 class="form-section-header">What is your target time for this race?</h4>
        <textarea></textarea>

        <button type="submit" class="button accent">Continue</button>
    </div>
<% end %>

<%= form_for(create_goal_path, html: {id: "new-goal", autocomplete: "off", :class => "form"}) do |f| %>
    <div class="form-section">
        <h4 class="form-section-header">Enter your personal info.</h4>
        <p class="note">Only your name will be visible to the public.</p>
        <textarea></textarea>

        <button type="submit" class="button accent">Continue</button>
    </div>
<% end %>