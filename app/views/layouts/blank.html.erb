<!DOCTYPE <!doctype html>
<html lang="en" id="blank">
<%= render "head" %>
<body>
    <div id="header-wrap">
        <a href="<%= root_path %>" id="logo">
            <img src="<%= asset_path('logo_white.png') %>"/>
        </a>
    </div>
    <div id="center-wrap">
        <%= yield %>
    </div>
</body>
</html>